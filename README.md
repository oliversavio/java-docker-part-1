Sample code to go along with my blog post:

### Building the jar
`mvn clean package`

This will create a jar named `hellodocker-1.0-SNAPSHOT-shaded.jar` in the target directory.

### Running the application
`java -jar hellodocker-1.0-SNAPSHOT-shaded.jar`